. ترتیب اجرا: ابتدا توابع کمکی (fn_check_balance, fn_refund_from_escrow, fn_deposit_fee, fn_deposit_platform_fee) را اجرا کنید.
3. سپس بقیه توابع را به ترتیب دلخواه اجرا کنید (وابستگی ندارند).
4. برای اجرای خودکار توابع منقضی‌شده، یک Edge Function در Supabase تنظیم کنید که هر دقیقه توابع زیر را صدا بزند:
   · SELECT fn_cancel_expired_duels();
   · SELECT fn_cancel_expired_challenges();

1. اکستنشن‌ها: مطمئن شوید که اکستنشن‌های pg_cron و pg_net در Supabase فعال هستند (از طریق دشبورد یا SQL).
2. پر کردن اطلاعات: در محیط‌های جدید، اطلاعات پروژه (project_ref و service_role_key) باید در تنظیمات Supabase تنظیم شوند











پیاده‌سازی i18n (بین‌المللی‌سازی)
npm install i18next react-i18next i18next-browser-languagedetector


تولید تصویر با کد (آواتار)
npm install @dicebear/avatars @dicebear/avataaars

آیکون های UI
npm install lucide-react


# نصب Sharp (کتابخانه پردازش تصویر در Node.js)
npm install -g sharp-cli

# یا برای استفاده در پروژه:
cd dobna
npm install sharp



# در ریشه پروژه (dobna/)
node compress-images.js


npm install @supabase/supabase-js