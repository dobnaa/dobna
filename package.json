{
  "name": "dobna-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "DOBNA Monorepo - Social + Game + Exchange",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build",
    "test": "pnpm run --parallel test",
    "lint": "pnpm run --parallel lint",
    "clean": "pnpm run --parallel clean && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}